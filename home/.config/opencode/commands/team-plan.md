---
description: Plan workflow with explicit approval gate
agent: team
---

Use the team agent workflow in `home/.config/opencode/agents/team.md`.

Plan request: $ARGUMENTS

Keep this as planning-only: present the plan and run the approval gate (`Approve this plan?` with `Approve` / `Request changes`) before any execution.
